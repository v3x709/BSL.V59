# BSL.v59 Python Rebuild
A 1:1 translation of the BSL.v59 C# server to Python.

## Installation
1. Install Python and libsodium: `pkg install python libsodium`
2. Install pysodium: `pip install pysodium`
3. Run `./start.sh`
